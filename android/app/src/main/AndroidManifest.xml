<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Core -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Android 14+ requires a typed foreground service; the VPN uses
         specialUse (DNS-level filtering is its exact purpose). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Required for the call-style Focus Session notification (the
         status-bar duration chip) on several Android versions. -->
    <uses-permission android:name="android.permission.MANAGE_OWN_CALLS" />

    <!-- Usage access (special, user-granted in system Settings → Usage
         access): lets the app read UsageStatsManager's authoritative
         per-app foreground times so the dashboard and charts render
         exact screen time — the same source Digital Wellbeing uses. -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <!-- Bedtime alarms. USE_EXACT_ALARM is auto-granted; the
         SCHEDULE_EXACT_ALARM fallback keeps 12/12L devices working via
         the inexact path when the user hasn't granted exact access. -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- Android 13+ runtime permission for notification batching/muting -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Needed to enumerate installed apps for the app-picker in
         Limits/Blocking screens. -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <application
        android:label="Ulimit"
        android:name=".UlimitApplication"
        android:icon="@mipmap/ic_launcher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Accessibility Service — the core enforcement engine. Detects
             foreground app changes (usage tracking, limit enforcement)
             and draws the blocking overlay directly. -->
        <service
            android:name=".UlimitAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Device Admin — blocks uninstall/force-stop as a bypass route. -->
        <receiver
            android:name=".UlimitDeviceAdminReceiver"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:exported="true">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_receiver" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

        <!-- Notification Listener — powers notification holding during
             focus sessions. -->
        <service
            android:name=".UlimitNotificationListenerService"
            android:label="Ulimit"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Local VPN — per-app internet blocking and website/domain
             filtering. Traffic never leaves the device. -->
        <service
            android:name=".UlimitVpnService"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Local on-device DNS and app network filtering" />
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
        </service>

        <!-- Focus Session system indicator: foreground service with an
             ongoing chronometer notification and Pause/End actions. -->
        <service
            android:name=".FocusIndicatorService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Focus session timer indicator" />
        </service>

        <!-- Chip tap target: the translucent spring-bouncy popup that
             drops from the status-bar Focus Session chip. Special launch
             mode so tapping the chip can't clear the app task. -->
        <activity
            android:name=".FocusSessionPopupActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:taskAffinity=""
            android:launchMode="singleInstance"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <!-- System-notification actions (Pause/Resume/End) for the focus
             indicator, routed into the Dart FocusController. -->
        <receiver
            android:name=".FocusSessionActionReceiver"
            android:exported="false" />

        <!-- Restores enforcement work after a reboot. -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Bedtime window edges (DND on/off, VPN nudge). -->
        <receiver
            android:name=".BedtimeAlarmReceiver"
            android:exported="false" />

    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>
</manifest>
